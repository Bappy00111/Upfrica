import React from 'react';

const FreeDeliveryInfo = ({delivery}) => {
    return (
        <div>
            <h1>Delivery: $ {delivery}</h1>
        </div>
    );
};

export default FreeDeliveryInfo;